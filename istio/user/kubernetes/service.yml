# API version
apiVersion: v1

# resource type
kind: Service

# metadata of the service
metadata:
  name: user

# specification
spec:
  type: ClusterIP

  ports:
    - name: http
      port: 9999
      protocol: TCP
      targetPort: 9999

  selector:
    app: user
