# API version
apiVersion: v1

# resource type
kind: Service

# metadata of the service
metadata:
  name: comments

# specification
spec:
  type: ClusterIP

  ports:
    - name: http
      port: 9998
      protocol: TCP
      targetPort: 9998

  selector:
    app: comments
